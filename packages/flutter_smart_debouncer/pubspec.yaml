name: flutter_smart_debouncer
version: 1.0.0

description: A unified package providing debouncing utilities and smart Flutter widgets (TextField, Button, etc.).
repository: https://github.com/example/flutter_smart_debouncer
issue_tracker: https://github.com/example/flutter_smart_debouncer/issues
homepage: https://github.com/example/flutter_smart_debouncer

topics:
  - debounce
  - widget
  - utils
  - flutter

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.10.0

dev_dependencies:
  fake_async: ^1.3.1
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter
  test: ^1.24.0

flutter:
  uses-material-design: true
